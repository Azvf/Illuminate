# Compact 模式模板

适用：单一职责、单一链路的小模块。

输出单文件 `docs/Framework/<Module>.md`：

```markdown
# 模块名称

## 职责与边界

<一句话描述模块做什么，不做什么。>

## 入口

- `<入口函数或类>` — <说明>
- `<恢复入口>` — <说明>

## 核心链路

```
<组件A> → <组件B> → <组件C>
```

## 关键状态

- <状态> 由 <组件> 管理。

## 风险与维护

- <风险项1>
- <风险项2>

## 实现锚点

- `<源文件路径>`
```
