interface:
  display_name: "Archive Module Documentation"
  short_description: "Organize verified knowledge for one module into a compact, standard, or extended Framework documentation set"
  default_prompt: "Archive verified knowledge for one module into the complete human-readable 30-modules/<module>/README.md, with optional linked platform/topic docs and verification YAML doc_refs"

policy:
  allow_implicit_invocation: false
